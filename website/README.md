# TTC Delays (application)

The web application lives in this folder (`frontend` + `api` + `data`).

## Run locally

From this directory (`website/`):

```bash
npm run setup   # first time only
npm run dev
```

Open **http://localhost:5173**.

For full steps and requirements, see the [project README](../README.md#run-locally) at the repository root.
